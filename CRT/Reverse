#include <stdio.h>

int main()
{
    int num,rev=0,rem;
    printf("enter num:");
    scanf("%d",&num);
    int temp=num;
    while(num!=0)
    {
        rem=num%10;
        rev=rev*10+rem;//prinf("%d",rem)
        num/=10;
    }
    printf("Reversed number:%d\n",rev);
    

    return 0;
}
