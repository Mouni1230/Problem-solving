#include <stdio.h>

int main()
{
    int main()
{
    int n;
    printf("Enter values in array:");
    scanf("%d",&n);
    int arr[n];
    
    for(int i=1;i<=n;i++)
    {
        scanf("%d",&arr[i]);
    }
    int big=arr[0],big2=-1;
    for(int j=1;j<=n;j++)
    {
        if(arr[j]>big)
        {
            big2=big;
            big=arr[j];
        }
        else if(arr[i]>big2 && arr[j]!=big)
        {
            big2=
        }
    }
  printf("The Biggest element is :%d",big);
    return 0;
    
    return 0;
}
